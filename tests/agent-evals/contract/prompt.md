Use the `$migrate-spring-to-liberty` skill at `{skill_path}` for the application in `{workspace}`.

The user has already confirmed this migration contract:

{contract}

Do not ask questions already answered by the contract. Create the deterministic inventory and characterization contract before changing production code. Follow every adapter and stop safely at `PARTIAL` or `BLOCKED` when the requested boundary cannot be proven. Create `migration-report.md`, run every safe applicable build/test/runtime check, cleanly stop owned processes, and do not commit or push.
